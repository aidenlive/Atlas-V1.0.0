# Decisions: 02 Rebrand to Atlas and ship a first-class CLI

ADRs scoped to this workstream, named `NNNN-short-title.md`. Same rules as the
repository's `docs/decisions/`: append-only, superseded rather than edited.

A decision that outlives this workstream is promoted to `docs/decisions/` with
a link back here (W-07).

*Intentionally empty until the first decision is made.*
