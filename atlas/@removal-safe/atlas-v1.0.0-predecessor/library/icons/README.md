# Icons

Interface glyphs. One concept per file, drawn on a 24px grid, `currentColor` only,
no text inside the glyph. The accessible name is written at the use site, because
the same glyph is "close" in one control and "dismiss" in another (L-I1–L-I4).

Index: [`index.yaml`](index.yaml) · Standard: [`spec/library.md`](../../spec/library.md)

This class is currently empty. The index is the place to start.
