# Media

Diagrams, screenshots, and recordings, each recorded against the thing that
produced it. A raster with no source is a dead end the next person cannot edit.
If a script can emit it, a script should: then it is drift-checked like
everything else (L-M1–L-M3).

Index: [`index.yaml`](index.yaml) · Standard: [`spec/library.md`](../../spec/library.md)

This class is currently empty. The index is the place to start.
