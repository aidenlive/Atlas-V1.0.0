# lexicon

One file — [`terms.yaml`](terms.yaml) — decides how we spell our names and which
habits of writing we have decided against. `atlas lint` reads it directly.

```bash
atlas lexicon find email
atlas lexicon phrases
```

How to read an entry, and how to propose a change: see
[the lexicon reference](../../docs/reference/lexicon.md).
